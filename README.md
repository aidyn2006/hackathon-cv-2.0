# Digital Inspector 🔍

Система автоматической проверки документов с детекцией подписей, штампов и QR-кодов на базе YOLOv8.

## 🚀 Быстрый старт с Docker

### Запуск одной командой

```bash
./docker-start.sh
```

Или:

```bash
docker-compose up -d
```

### Доступ к приложению

- **URL:** http://localhost:5002
- **Логин:** `inspector`
- **Пароль:** `demo123`

## 📋 Требования

- Docker 20.10+
- Docker Compose 2.0+
- 4GB RAM minimum
- Файл `best.pt` (YOLO модель) - должен быть в корне проекта

## 🐳 Docker команды

```bash
# Запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Пересборка
docker-compose up -d --build
```

## 📦 Что включает контейнер

- ✅ Flask Backend + Frontend
- ✅ SQLite База данных
- ✅ YOLOv8 модель для детекции
- ✅ Preprocessing (обработка изображений)
- ✅ Postprocessing (фильтрация результатов)
- ✅ QR-код декодер
- ✅ PDF конвертер

## 🎨 Цветовая схема детекций

- 🟢 **Green** = Signature / Подпись
- 🟣 **Purple** = Stamp / Штамп
- 🟡 **Yellow** = QR Code

## 📥 Поддерживаемые форматы

- PDF (многостраничные)
- PNG, JPG, JPEG

## 🔧 Альтернативный запуск (без Docker)

```bash
# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить
python app_inspector.py
```

## 📊 Возможности

- Детекция подписей (порог confidence: 0.20)
- Детекция штампов (порог confidence: 0.25)
- Детекция и декодирование QR-кодов
- Обработка многостраничных PDF
- Side-by-side просмотр (оригинал vs обработанный)
- Скачивание PDF с аннотациями
- Export результатов в JSON

## 🔐 Пользователи по умолчанию

```
inspector / demo123
admin / admin123
user / user123
```

## 📁 Структура данных

```
uploads/        # Загруженные документы
annotated/      # Обработанные изображения с bbox
digital_inspector.db  # База данных
best.pt         # YOLO модель
```

## ⚙️ Конфигурация

Основные параметры в `app_inspector.py`:

- **Port:** 5002
- **Max file size:** 50 MB
- **YOLO conf:** 0.20
- **YOLO imgsz:** 640
- **IoU threshold:** 0.5

## 🐛 Troubleshooting

**Порт занят:**
```bash
# Измените порт в docker-compose.yml
ports:
  - "5003:5002"
```

**Модель не найдена:**
```bash
# Убедитесь что best.pt в корне проекта
ls -lh best.pt
```

**Ошибка разрешений:**
```bash
chmod -R 777 uploads/ annotated/
```

## 📄 Лицензия

MIT

## 👨‍💻 Разработка

HackNU 2025
