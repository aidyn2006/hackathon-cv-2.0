# Веб-приложение для детекции QR-кодов, подписей и штампов

Веб-приложение для загрузки PDF файлов и автоматического обнаружения QR-кодов, подписей и штампов с помощью обученной YOLO модели.

## Возможности

- 📄 Загрузка PDF файлов
- 🔍 Автоматическая детекция QR-кодов
- ✍️ Обнаружение подписей
- 🏷️ Обнаружение штампов
- 📊 Визуализация результатов с выделением найденных объектов
- 📱 Адаптивный дизайн

## Требования

- Python 3.8+
- poppler (для конвертации PDF в изображения)

### Установка poppler

**macOS:**
```bash
brew install poppler
```

**Ubuntu/Debian:**
```bash
sudo apt-get install poppler-utils
```

**Windows:**
Скачайте и установите poppler с [официального сайта](https://github.com/oschwartz10612/poppler-windows/releases/)

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Убедитесь, что файл модели `best.pt` находится в корневой директории проекта.

## Запуск

```bash
python app.py
```

Приложение будет доступно по адресу: http://localhost:5001

## Использование

1. Откройте браузер и перейдите на http://localhost:5001
2. Перетащите PDF файл в область загрузки или нажмите для выбора файла
3. Дождитесь обработки документа
4. Просмотрите результаты детекции с выделенными объектами

## Структура проекта

```
hacknu/
├── app.py              # Flask приложение
├── best.pt             # Обученная YOLO модель
├── requirements.txt    # Зависимости Python
├── templates/          # HTML шаблоны
│   └── index.html
├── static/             # Статические файлы (CSS, JS)
│   └── style.css
└── README.md           # Документация
```

## Технологии

- **Flask** - веб-фреймворк
- **Ultralytics YOLO** - модель детекции объектов
- **OpenCV** - обработка изображений
- **pdf2image** - конвертация PDF в изображения
- **pyzbar** - детекция QR-кодов

## Примечания

- Максимальный размер загружаемого файла: 16MB
- Поддерживаются только PDF файлы
- Модель должна быть обучена на классах: подписи, штампы и другие объекты

