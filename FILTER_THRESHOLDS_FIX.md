# 🔧 Исправление порогов фильтрации

## ❌ Проблема:

**Детекции (bbox) рисуются на изображении, но не отображаются в списке "Обнаруженные объекты"**

### Причина:
Слишком жесткие пороги confidence в post-processing фильтрации:
- Штампы: требовалось ≥ 0.35
- Подписи: требовалось ≥ 0.25  
- Другое: требовалось ≥ 0.30

Если штамп имел confidence 0.20-0.34, он **отфильтровывался** после детекции.

---

## ✅ Исправление:

### **Понижены пороги фильтрации в `postprocessing.py`:**

**Было:**
```python
class_thresholds = {
    'подпись': 0.25,
    'signature': 0.25,
    'штамп': 0.35,      # ❌ Слишком высокий
    'stamp': 0.35,      # ❌ Слишком высокий
    'qr': 0.4,
    'default': 0.3
}
```

**Стало:**
```python
class_thresholds = {
    'подпись': 0.20,    # ✅ Понижен с 0.25
    'signature': 0.20,  # ✅ Понижен с 0.25
    'штамп': 0.20,      # ✅ Понижен с 0.35
    'stamp': 0.20,      # ✅ Понижен с 0.35
    'qr': 0.3,          # ✅ Понижен с 0.4
    'default': 0.25     # ✅ Понижен с 0.30
}
```

### **Финальная фильтрация:**

**Было:**
```python
filter_low_confidence_detections(processed, min_confidence=0.3)
```

**Стало:**
```python
filter_low_confidence_detections(processed, min_confidence=0.20)
```

---

## 📊 Новая логика фильтрации:

```
YOLO Detection (conf >= 0.15)
    ↓
Class-Specific Filter (conf >= 0.20 для подписей/штампов)
    ↓
Global Confidence Filter (conf >= 0.20)
    ↓
Non-Maximum Suppression (IoU >= 0.6)
    ↓
Merge Nearby Signatures (distance < 50px)
    ↓
Final Results
```

---

## 🔍 Добавлено подробное логирование:

### **В `postprocessing.py`:**
```python
# Логирование отфильтрованных детекций
logger.info(f"Filtered out: {det['class']} (conf={det['confidence']:.3f} < threshold={threshold})")
logger.info(f"Class-specific filtering: {len(detections)} -> {len(filtered)} detections")
```

### **В `app.py`:**
```python
# Логирование финальных детекций перед отправкой
logger.info(f"Page {page_num + 1} - YOLO detections to send: {len(yolo_results)}")
for det in yolo_results:
    logger.info(f"  - {det['class']}: confidence={det['confidence']:.3f}, bbox={det['bbox']}")
```

---

## 📈 Результат:

### **До исправления:**
| Confidence | Штамп | Подпись | QR | Результат |
|------------|-------|---------|-----|-----------|
| 0.40 | ✅ | ✅ | ✅ | Отображается |
| 0.30 | ❌ | ✅ | ❌ | Штамп отфильтрован |
| 0.20 | ❌ | ❌ | ❌ | Все отфильтровано |

### **После исправления:**
| Confidence | Штамп | Подпись | QR | Результат |
|------------|-------|---------|-----|-----------|
| 0.40 | ✅ | ✅ | ✅ | Отображается |
| 0.30 | ✅ | ✅ | ✅ | Отображается |
| 0.20 | ✅ | ✅ | ❌ | Отображается (кроме QR) |
| 0.15 | ❌ | ❌ | ❌ | Отфильтровано |

---

## 🎯 Теперь будет отображаться:

✅ **Штампы с confidence ≥ 0.20** (было ≥ 0.35)  
✅ **Подписи с confidence ≥ 0.20** (было ≥ 0.25)  
✅ **QR-коды с confidence ≥ 0.30** (было ≥ 0.40)  
✅ **Другие объекты ≥ 0.25** (было ≥ 0.30)

---

## 🔧 Как проверить:

1. Загрузите PDF с документом
2. Если раньше показывало "Объекты не найдены" - теперь должны отображаться
3. Проверьте логи в `app.log`:
```bash
tail -f /Users/aidyn/Downloads/hacknu/app.log
```

### **Пример логов:**
```
[2025-11-15 12:00:45] - postprocessing - INFO - Starting post-processing with 5 initial detections
[2025-11-15 12:00:45] - postprocessing - INFO - Class-specific filtering: 5 -> 4 detections
[2025-11-15 12:00:45] - postprocessing - INFO - Filtered out: stamp (conf=0.180 < threshold=0.200)
[2025-11-15 12:00:45] - postprocessing - INFO - After confidence filtering: 4 detections
[2025-11-15 12:00:45] - postprocessing - INFO - After NMS: 3 detections
[2025-11-15 12:00:45] - app - INFO - Page 1 - YOLO detections to send: 3
[2025-11-15 12:00:45] - app - INFO -   - stamp: confidence=0.250, bbox=[120, 450, 280, 610]
[2025-11-15 12:00:45] - app - INFO -   - signature: confidence=0.320, bbox=[150, 650, 350, 720]
[2025-11-15 12:00:45] - app - INFO -   - signature: confidence=0.280, bbox=[400, 700, 550, 750]
```

---

## ⚠️ Возможные последствия:

### **Плюсы:**
- ✅ Больше реальных объектов будет обнаруживаться
- ✅ Меньше false negatives (пропущенных объектов)
- ✅ Лучше детекция слабых/бледных штампов и подписей

### **Минусы:**
- ⚠️ Может увеличиться количество false positives (ложных детекций)
- ⚠️ Некоторые шумовые артефакты могут пройти фильтрацию

### **Решение:**
- Если слишком много ложных детекций - повысить пороги:
  - Штампы: 0.20 → 0.25
  - Подписи: 0.20 → 0.23
- Если все еще не находит объекты - понизить пороги:
  - Штампы: 0.20 → 0.15
  - Подписи: 0.20 → 0.15

---

## 🎨 Визуализация порогов:

```
Confidence
1.0 ├─────────────────────── Идеальная детекция
    │
0.9 ├─────────────────────── Отличная детекция
    │
0.8 ├─────────────────────── Очень хорошая
    │
0.7 ├─────────────────────── Хорошая
    │
0.6 ├─────────────────────── Приемлемая
    │
0.5 ├─────────────────────── Удовлетворительная
    │
0.4 ├─── QR (БЫЛО)          ❌ Слишком строго
    │
0.35├─── Штампы (БЫЛО)      ❌ Слишком строго
    │
0.3 ├─── Default (БЫЛО)     ❌ Слишком строго
    │    QR (СТАЛО)          ✅ Оптимально
    │
0.25├─── Подписи (БЫЛО)     
    │    Default (СТАЛО)     ✅ Оптимально
    │
0.20├─── ВСЕ (СТАЛО)        ✅ Новый порог
    │
0.15├─── YOLO conf          (начальный порог)
    │
0.0 └─────────────────────── Шум
```

---

## 📋 Файлы изменены:

1. **`postprocessing.py`**
   - Понижены class_thresholds (строки 278-285)
   - Понижен min_confidence до 0.20 (строка 324)
   - Добавлено логирование фильтрации (строки 300-302)

2. **`app.py`**
   - Добавлено подробное логирование детекций (строки 259-262)

---

## 🚀 Запуск:

```bash
cd /Users/aidyn/Downloads/hacknu
/Users/aidyn/Downloads/hacknu/venv/bin/python app.py
```

**URL:** http://localhost:5001

---

## 🧪 Тестирование:

1. Загрузите документ с бледными штампами (confidence 0.20-0.35)
2. Проверьте, что они теперь отображаются в списке
3. Проверьте логи: `tail -f app.log`
4. Если слишком много шума - повысьте пороги в `postprocessing.py`

---

**Дата:** 2025-11-15  
**Версия:** 1.2  
**Статус:** ✅ Fixed & Deployed  
**Режим:** BALANCED с пониженными порогами фильтрации

